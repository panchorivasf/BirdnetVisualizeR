% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/birdnet_histogram.R
\name{birdnet_histogram}
\alias{birdnet_histogram}
\title{Create a histogram of species observation counts}
\usage{
birdnet_histogram(
  data,
  y_var = "species",
  top_n = 5,
  bins = 100,
  title = "",
  x_label = "Number of Detections",
  y_label = "auto",
  exp_x_factor = 1.3,
  save_png = TRUE
)
}
\arguments{
\item{data}{Data frame containing species observation data}

\item{y_var}{Character. Name of column containing taxa name (default: "species")}

\item{top_n}{Integer. Number of top species to label (0 for no labels, default: 5)}

\item{bins}{Integer. Number of bins for histogram (default: 30)}

\item{title}{Character. Plot title (default: "Species Count Distribution")}

\item{x_label}{Character. X-axis label (default: "Number of Observations")}

\item{y_label}{Character. Y-axis label (default: "auto")}

\item{save_png}{Logical. Whether to save plot as png.}

\item{x_exp_factor}{Numeric. Expansion factor for the right side of the
x-axis, used to accommodate species labels.}
}
\value{
A histogram plot showing distribution of species observation counts.
        Returns ggplot object for "ggplot2", plotly object for "plotly",
        or base R plot for "base".
}
\description{
Create a histogram of species observation counts
}
\details{
Creates a histogram of species observation counts with optional
         labeling of top-n most observed species. The function automatically
         loads required packages based on the selected plot type.
}
\examples{
\dontrun{
# Using default parameters
species_histogram(observation_data)

# With ggplot2 and custom labels
species_histogram(observation_data, plot_type = "ggplot2", top_n = 3)
}

\dontrun{
birdnet_histogram(all_data_geotax_clean,
                 y_var = "family",
                 top_n = 5,
                 exp_x_factor = 1.2)
}
}
